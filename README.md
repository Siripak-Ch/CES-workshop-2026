# Frontend GitHub Pages v6

Mobile-first static frontend. It renders immediately from local `assets/js/config.js` and sends one hidden form POST only when the user confirms Check-in.

The tab icon and in-page logo are local copies generated from the provided Google Drive PNG, so the page does not depend on a Drive hotlink.

# Frontend v12

- Fixed 2480 × 877 Canvas generator
- Custom vector icons only
- Smaller automatic-fit typography
- Drive save requires actual SUCCESS confirmation
- Retry button appears on save failure

Backend v12 must also be deployed because the frontend verifies save status through JSONP polling.
